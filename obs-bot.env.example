# OBS Bot Environment Variables
# Copy to /etc/obs-bot.env and customize for your system
#
# Installation:
#   sudo cp obs-bot.env.example /etc/obs-bot.env
#   sudo chmod 600 /etc/obs-bot.env
#   sudo chown streaming:streaming /etc/obs-bot.env
#   sudo nano /etc/obs-bot.env  # Edit with your values

# ==============================================================================
# OBS WebSocket Configuration (REQUIRED)
# ==============================================================================

# OBS WebSocket URL (default: ws://localhost:4455)
# For WSL2, use Windows host IP: ws://172.x.x.x:4455
OBS_BOT_OBS__WEBSOCKET_URL=ws://localhost:4455

# OBS WebSocket password (if configured in OBS)
# Leave empty if no password set in OBS Studio
OBS_WEBSOCKET_PASSWORD=

# ==============================================================================
# Twitch Streaming Configuration (REQUIRED)
# ==============================================================================

# Twitch stream key from https://dashboard.twitch.tv/settings/stream
# WARNING: Keep this secret! Do not commit to git!
TWITCH_STREAM_KEY=live_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ==============================================================================
# Discord Alerting Configuration (OPTIONAL but RECOMMENDED)
# ==============================================================================

# Discord webhook URL for critical alerts
# Create in Discord: Server Settings → Integrations → Webhooks
# Alerts include: stream offline, validation failures, failover events
DISCORD_WEBHOOK_URL=

# ==============================================================================
# Content Configuration (OPTIONAL)
# ==============================================================================

# Content directory path (Linux path inside container/system)
# Default: /app/content
# CONTENT_DIR=/app/content

# Windows content path for OBS media sources (WSL2 only)
# Format: //wsl.localhost/Debian/home/streaming/OBS_bot/content
# Default: //wsl.localhost/Debian/home/streaming/OBS_bot/content
# WINDOWS_CONTENT_PATH=//wsl.localhost/Debian/home/streaming/OBS_bot/content

# Failover video file path
# Default: /app/content/failover/default_failover.mp4
# FAILOVER_VIDEO=/app/content/failover/default_failover.mp4

# ==============================================================================
# Logging Configuration (OPTIONAL)
# ==============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
# LOG_LEVEL=INFO

# Log format: json, text
# Default: json
# LOG_FORMAT=json

# ==============================================================================
# Advanced Configuration (OPTIONAL)
# ==============================================================================

# OBS connection timeout (seconds)
# Default: 10
# OBS_CONNECTION_TIMEOUT_SEC=10

# OBS reconnect interval (seconds)
# Default: 2
# OBS_RECONNECT_INTERVAL_SEC=2

# Maximum OBS reconnect attempts
# Default: 3
# OBS_MAX_RECONNECT_ATTEMPTS=3

# Health metrics collection interval (seconds)
# Default: 10
# HEALTH_METRICS_INTERVAL_SEC=10

# Failover monitoring interval (seconds)
# Default: 15 (recommended: 5 for strict SC-005 compliance)
# FAILOVER_MONITORING_INTERVAL_SEC=15
